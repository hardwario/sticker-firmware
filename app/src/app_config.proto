syntax = "proto3";

// BEGIN GENERATED CONFIG
message AppConfigMessage {
    optional bool factory = 1;
    optional string secret_key = 2;
    optional uint32 serial_number = 3;
    optional uint32 nonce_counter = 4;
    optional Lorawan lorawan = 5;
    optional Application application = 6;

    message Lorawan {
        enum Region {
            EU868 = 0;
            US915 = 1;
            AU915 = 2;
        }

        enum Network {
            PUBLIC = 0;
            PRIVATE = 1;
        }

        enum Activation {
            OTAA = 0;
            ABP = 1;
        }

        optional Region region = 1;
        optional Network network = 2;
        optional bool adr = 3;
        optional Activation activation = 4;
        optional string deveui = 5;
        optional string joineui = 6;
        optional string nwkkey = 7;
        optional string appkey = 8;
        optional string devaddr = 9;
        optional string nwkskey = 10;
        optional string appskey = 11;
        optional uint32 sub_band = 12;
    }

    message Application {
        reserved 3;
        optional bool calibration = 1;
        optional uint32 interval_sample = 2;
        optional uint32 interval_report = 4;
        optional bool alarm_temperature_enabled = 5;
        optional float alarm_temperature_lo = 6;
        optional float alarm_temperature_hi = 7;
        optional float alarm_temperature_hst = 8;
        optional bool alarm_humidity_enabled = 9;
        optional float alarm_humidity_lo = 10;
        optional float alarm_humidity_hi = 11;
        optional float alarm_humidity_hst = 12;
        optional bool alarm_pressure_enabled = 13;
        optional float alarm_pressure_lo = 14;
        optional float alarm_pressure_hi = 15;
        optional float alarm_pressure_hst = 16;
        optional bool alarm_t1_temperature_enabled = 17;
        optional float alarm_t1_temperature_lo = 18;
        optional float alarm_t1_temperature_hi = 19;
        optional float alarm_t1_temperature_hst = 20;
        optional bool alarm_t2_temperature_enabled = 21;
        optional float alarm_t2_temperature_lo = 22;
        optional float alarm_t2_temperature_hi = 23;
        optional float alarm_t2_temperature_hst = 24;
        optional bool hall_left_counter = 25;
        optional bool hall_left_notify_act = 26;
        optional bool hall_left_notify_deact = 27;
        optional bool hall_right_counter = 28;
        optional bool hall_right_notify_act = 29;
        optional bool hall_right_notify_deact = 30;
        optional bool input_a_counter = 31;
        optional bool input_a_notify_act = 32;
        optional bool input_a_notify_deact = 33;
        optional bool input_b_counter = 34;
        optional bool input_b_notify_act = 35;
        optional bool input_b_notify_deact = 36;
        optional float corr_temperature = 37;
        optional float corr_t1_temperature = 38;
        optional float corr_t2_temperature = 39;
        optional bool cap_hall_left = 40;
        optional bool cap_hall_right = 41;
        optional bool cap_input_a = 42;
        optional bool cap_input_b = 43;
        optional bool cap_light_sensor = 44;
        optional bool cap_barometer = 45;
        optional bool cap_pir_detector = 46;
        optional bool cap_1w_thermometer = 47;
        optional bool cap_1w_machine_probe = 48;
        optional bool history_enable = 49;
        optional uint32 history_sensors = 50;
        optional uint32 alarm_limit = 51;
        optional uint32 alarm_notif_time = 52;
        optional bool pir_notify_act = 53;
        optional string sensor1_rom = 54;
        optional uint32 sensor1_type = 55;
        optional uint32 sensor1_sht = 56;
        optional string sensor2_rom = 57;
        optional uint32 sensor2_type = 58;
        optional uint32 sensor2_sht = 59;
        optional string sensor3_rom = 60;
        optional uint32 sensor3_type = 61;
        optional uint32 sensor3_sht = 62;
        optional string sensor4_rom = 63;
        optional uint32 sensor4_type = 64;
        optional uint32 sensor4_sht = 65;
        optional bool alarm_s1_temperature_enabled = 66;
        optional float alarm_s1_temperature_lo = 67;
        optional float alarm_s1_temperature_hi = 68;
        optional float alarm_s1_temperature_hst = 69;
        optional bool alarm_s1_humidity_enabled = 70;
        optional float alarm_s1_humidity_lo = 71;
        optional float alarm_s1_humidity_hi = 72;
        optional float alarm_s1_humidity_hst = 73;
        optional bool alarm_s1_tilt_enabled = 74;
        optional float corr_s1_temperature = 75;
        optional bool alarm_s2_temperature_enabled = 76;
        optional float alarm_s2_temperature_lo = 77;
        optional float alarm_s2_temperature_hi = 78;
        optional float alarm_s2_temperature_hst = 79;
        optional bool alarm_s2_humidity_enabled = 80;
        optional float alarm_s2_humidity_lo = 81;
        optional float alarm_s2_humidity_hi = 82;
        optional float alarm_s2_humidity_hst = 83;
        optional bool alarm_s2_tilt_enabled = 84;
        optional float corr_s2_temperature = 85;
        optional bool alarm_s3_temperature_enabled = 86;
        optional float alarm_s3_temperature_lo = 87;
        optional float alarm_s3_temperature_hi = 88;
        optional float alarm_s3_temperature_hst = 89;
        optional bool alarm_s3_humidity_enabled = 90;
        optional float alarm_s3_humidity_lo = 91;
        optional float alarm_s3_humidity_hi = 92;
        optional float alarm_s3_humidity_hst = 93;
        optional bool alarm_s3_tilt_enabled = 94;
        optional float corr_s3_temperature = 95;
        optional bool alarm_s4_temperature_enabled = 96;
        optional float alarm_s4_temperature_lo = 97;
        optional float alarm_s4_temperature_hi = 98;
        optional float alarm_s4_temperature_hst = 99;
        optional bool alarm_s4_humidity_enabled = 100;
        optional float alarm_s4_humidity_lo = 101;
        optional float alarm_s4_humidity_hi = 102;
        optional float alarm_s4_humidity_hst = 103;
        optional bool alarm_s4_tilt_enabled = 104;
        optional float corr_s4_temperature = 105;
    }
}
// END GENERATED CONFIG

message Command {
    uint32 seq = 1;

    oneof body {
        SetParam        set_param        = 2;
        GetParam        get_param        = 3;
        GetInfo         get_info         = 4;
        GetConfig       get_config       = 5;
        SettingsSave    settings_save    = 6;
        Reboot          reboot           = 7;
        FactoryReset    factory_reset    = 8;
        ForceSend       force_send       = 9;
        ResetCounters   reset_counters   = 10;
        ReqHistory      req_history      = 11;
        ClockSync       clock_sync       = 12;
    }

    message SetParam {
        optional AppConfigMessage.Lorawan     lorawan     = 1;
        optional AppConfigMessage.Application application = 2;
    }

    message GetParam {
        repeated uint32 lorawan_field     = 1;
        repeated uint32 application_field = 2;
    }

    message GetInfo      { }
    message GetConfig    { optional uint32 page = 1; }
    message SettingsSave { }
    message Reboot       { }
    message FactoryReset { }
    message ForceSend    { }
    message ResetCounters {
        optional bool hall_left  = 1;
        optional bool hall_right = 2;
        optional bool input_a    = 3;
        optional bool input_b    = 4;
    }
    message ReqHistory   { optional uint32 from_unix = 1; optional uint32 to_unix = 2; }
    message ClockSync    { }
}

message Response {
    uint32 seq = 1;

    oneof body {
        Ack          ack           = 2;
        Info         info          = 3;
        ConfigDump   config_dump   = 4;
        HistoryFrame history_frame = 5;
        Error        error         = 6;
    }

    message Ack { }

    message Info {
        // Firmware version (semver components). board and dev_eui are omitted
        // on purpose — the LNS already knows them from the device context.
        uint32 fw_major = 1;
        uint32 fw_minor = 2;
        uint32 fw_patch = 3;

        // Source of the build (orthogonal to debug below).
        enum BuildType {
            MAIN   = 0;   // tagged build from main
            DEV    = 1;   // CI build from a branch / PR
            CUSTOM = 2;   // local / modified firmware
        }
        BuildType build_type = 4;

        uint32 serial_number = 5;
        uint32 uptime_s      = 6;   // seconds since boot
        uint32 unix_time     = 7;   // wall-clock (UTC); 0/absent until RTC synced
        bool   debug         = 8;   // true = debug build, false = release
    }

    message ConfigDump {
        uint32 page_index = 1;
        uint32 page_count = 2;
        optional AppConfigMessage.Lorawan     lorawan     = 3;
        optional AppConfigMessage.Application application = 4;
    }

    message HistoryFrame {
        uint32 frame_index = 1;
        uint32 frame_count = 2;
        uint32 t0_unix     = 3;
        bytes  samples     = 4;   // records, values only (no per-record time/mask)
        uint32 present     = 5;   // sensor mask shared by every record in the frame
        uint32 interval_s  = 6;   // seconds between records; time(j) = t0_unix + j*interval_s
    }

    message Error {
        enum Code {
            UNKNOWN             = 0;
            BAD_REQUEST         = 1;
            OUT_OF_RANGE        = 2;
            NOT_READY           = 3;
            HISTORY_UNAVAILABLE = 4;
            UNSUPPORTED_FIELD   = 5;
            PERSIST_FAILED      = 6;
        }

        Code   code         = 1;
        uint32 fault_field  = 2;
        string detail       = 3;
    }
}

// Alarm-detail batch, sent on fPort 3 (#27). Alarm edges (threshold + discrete)
// collected over one alarm_limit window are reported as a list; each event names
// its source, transition and (for thresholds) bound, plus a time offset and the
// current value. base_time is the window start (unix, or uptime-s before the RTC
// syncs); per-event time = base_time + rel_s. total counts every alarm in the
// window and may exceed events[] when records are dropped to fit the data rate.
message AlarmReport {
    uint32 base_time = 1;
    uint32 total     = 2;
    repeated AlarmEvent events = 3;
}

message AlarmEvent {
    // An alarm edge described by three orthogonal enums instead of one combined
    // flag: which sensor (source), the transition (edge), and — for thresholds —
    // which bound was crossed (side). Discrete sources (hall/input/PIR) report
    // SIDE_NONE; PIR is activate-only.

    // Sensor that raised the event. Values match enum app_alarm_source 1:1.
    enum Source {
        HALL_LEFT      = 0;
        HALL_RIGHT     = 1;
        PIR_MOTION     = 2;
        INPUT_A        = 3;
        INPUT_B        = 4;
        TEMPERATURE    = 5;
        HUMIDITY       = 6;
        PRESSURE       = 7;
        T1_TEMPERATURE = 8;
        T2_TEMPERATURE = 9;
    }

    // Transition direction. ACTIVATE = 0 so the common case omits the field.
    enum Edge {
        ACTIVATE   = 0;
        DEACTIVATE = 1;
    }

    // Which threshold bound was crossed; SIDE_NONE for discrete sources.
    enum Side {
        SIDE_NONE = 0;
        SIDE_LO   = 1;
        SIDE_HI   = 2;
    }

    Source source = 1;           // which sensor
    Edge   edge   = 2;           // activate / deactivate
    Side   side   = 3;           // hi / lo / none
    uint32 rel_s  = 4;           // seconds since AlarmReport.base_time
    optional sint32 value = 5;   // current value, scaled (×100 temp/hum, ×10 pressure); absent for discrete
}

// One ROM-bound 1-Wire sensor reading (fPort 2 telemetry, repeated in
// Telemetry.w1_sensors). `type` matches enum app_w1_slot_type (1=dallas,
// 2=machine-probe); quantities the slot's type doesn't provide are absent.
message SensorReading {
    uint32 slot                 = 1;  // 1-based logical slot
    uint32 type                 = 2;  // 1=dallas, 2=machine-probe
    optional sint32 temperature = 3;  // degC x100
    optional uint32 humidity    = 4;  // %RH  x2
    optional uint32 flags       = 5;  // bit0 tilt
}

// Periodic / event-triggered telemetry uplink, sent on fPort 2 (legacy bitmap
// stays on fPort 1). Fields are grouped per sensor; each group's boolean state
// lives in its own `*_flags` field so a sensor's data stays together as an
// atomic unit. A field is present only when its sensor is connected (capability
// on) and has valid data. The composer packs whole groups by priority into one
// or more frames to fit the LoRaWAN payload budget (split, never drop); all
// frames of a report carry the same snapshot. Scalars are scaled-int varints.
// The same message is emitted for periodic reports and event triggers.
message Telemetry {
    // system
    optional uint32 voltage          = 1;   // V    x50
    optional uint32 system_flags     = 2;   // bit0 boot
    // internal (SHT4x)
    optional sint32 temperature      = 3;   // degC x100
    optional uint32 humidity         = 4;   // %RH  x2
    // barometer (cap_barometer)
    optional uint32 pressure         = 5;   // hPa  x1000
    optional sint32 altitude         = 6;   // m    x10
    // light (cap_light_sensor)
    optional uint32 illuminance      = 7;   // lux  /2
    // accelerometer
    optional uint32 orientation      = 8;   // 1..6
    // pir (cap_pir_detector)
    optional uint32 motion_count     = 9;
    // 1-Wire per-slot readings moved to repeated w1_sensors (field 26); the
    // old flat ext1/ext2/mp1/mp2 fields are retired (v1.4.0 wire break).
    reserved 10, 11, 12, 13, 14, 15, 16, 17;
    // hall left (cap_hall_left)
    optional uint32 hall_left_count  = 18;
    optional uint32 hall_left_flags  = 19;  // bit0 notify_act, bit1 notify_deact, bit2 active
    // hall right (cap_hall_right)
    optional uint32 hall_right_count = 20;
    optional uint32 hall_right_flags = 21;
    // input A (cap_input_a)
    optional uint32 input_a_count    = 22;
    optional uint32 input_a_flags    = 23;  // bit0 notify_act, bit1 notify_deact, bit2 active
    // input B (cap_input_b)
    optional uint32 input_b_count    = 24;
    optional uint32 input_b_flags    = 25;
    // 1-Wire ROM-bound sensors (replaces ext1/ext2/mp1/mp2); one per present slot.
    repeated SensorReading w1_sensors = 26;
    // New sensors append at field >= 27 as new groups; never renumber.
}
