CONFIG_ZTEST=y
CONFIG_NANOPB=y
CONFIG_LOG=y
CONFIG_LOG_MODE_MINIMAL=y
CONFIG_TEST_RANDOM_GENERATOR=y

# app_compose.c's W1-slot encoding is now CONFIG_W1/CONFIG_DS28E17-gated
# (issue #395); this suite exercises that path against stubs.c, not a real
# driver, so just needs the Kconfig symbols defined.
CONFIG_W1=y
CONFIG_DS28E17=y
