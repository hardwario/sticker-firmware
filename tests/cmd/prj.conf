CONFIG_ZTEST=y
CONFIG_NANOPB=y
CONFIG_LOG=y
CONFIG_LOG_MODE_MINIMAL=y

# app_cmd.c's buzzer-play dispatch is now CONFIG_APP_BUZZER-gated (issue
# #395); this suite exercises that path against stubs, not a real driver.
CONFIG_APP_BUZZER=y
