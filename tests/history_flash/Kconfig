# Copyright (c) 2026 HARDWARIO a.s.
# SPDX-License-Identifier: Apache-2.0
#
# Mirror the app's history Kconfig symbols, but select the NVS flash backend
# so the coalescing / reboot-survival path is the one under test.

config APP_HISTORY_FLASH
	bool
	default y

config APP_HISTORY_BYTES
	int
	default 64

source "Kconfig.zephyr"
